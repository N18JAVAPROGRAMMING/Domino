<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.guest.domino">
<uses-permission android:room_name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:room_name=".MyApplication"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:room_name=".InActivity">
            <intent-filter>
                <action android:room_name="android.intent.action.MAIN" />

                <category android:room_name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:room_name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:room_name=".GameActivity"
            android:label="@string/title_activity_game" />
    </application>

</manifest>